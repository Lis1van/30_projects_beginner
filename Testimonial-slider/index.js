// Давайте рассмотрим JavaScript код проекта, который отвечает за автоматическое обновление отзывов (testimonials) на странице. Этот код создает простой слайдер, который циклично показывает отзывы, меняя их каждые 2 секунды. Вот его подробное объяснение по блокам:
//
//     ### Объявление и Инициализация
//
const testimonials = [
    {
        name: 'Lisvan',
        photoUrl: 'https://images.unsplash.com/photo-1535713875002-d1d0cf377fde?q=80&w=1780&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D',
        text: 'Я - энтузиаст веб-разработки, стремящийся постоянно развивать свои навыки и познания в этой динамичной и захватывающей области. Моя страсть к веб-технологиям зародилась еще в юности, когда я начал изучать HTML, CSS и JavaScript, создавая простые, но функциональные веб-сайты.'
    },
    {
        name: 'Georgiy',
        photoUrl: 'https://images.unsplash.com/flagged/photo-1573740144655-bbb6e88fb18a?q=80&w=1935&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D',
        text: 'Со временем мой интерес к веб-разработке только рос, и я погрузился в изучение современных фреймворков и библиотек, таких как React, Angular и Vue.js. Я научился создавать отзывчивые и интерактивные пользовательские интерфейсы, оптимизировать производительность веб-приложений и интегрировать их с серверными технологиями, такими как Node.js и базами данных.'
    },
    {
        name: 'Valik',
        photoUrl: 'https://images.unsplash.com/photo-1633332755192-727a05c4013d?q=80&w=1780&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D',
        text: 'Помимо фронтенд-разработки, я также овладел навыками бэкенд-программирования, изучив языки программирования, такие как Python и Java, а также фреймворки для создания веб-приложений, включая Django и Spring.'
    },
]
//
// - **`testimonials`**: Массив объектов, каждый из которых представляет отзыв. Каждый объект содержит:
//     - **`name`**: Имя пользователя.
// - **`photoUrl`**: Ссылка на фото пользователя.
// - **`text`**: Текст отзыва.
//
//     Эти данные будут циклически отображаться на странице.
//
// ### Получение элементов DOM
//
const imgEl = document.querySelector('img');
const textEl = document.querySelector('.text');
const userNameEl = document.querySelector('.user-name');
//
// - **`imgEl`**: Получает первый элемент `img` на странице, который используется для отображения фотографии пользователя.
// - **`textEl`**: Получает элемент с классом `.text`, используемый для отображения текста отзыва.
// - **`userNameEl`**: Получает элемент с классом `.user-name`, используемый для отображения имени пользователя.
//
//     Эти переменные связываются с элементами DOM, которые будут обновляться при каждом изменении отзыва.
//
// ### Инициализация Индекса и Обновление Отзывов
//
let idx = 0;
//
// - **`idx`**: Инициализация переменной `idx` с начальным значением `0`. Этот индекс используется для отслеживания текущего отзыва, который отображается.
//
updateTestimonials();
//
// - Вызов функции `updateTestimonials()` запускает процесс обновления отзывов при загрузке страницы.
//
// ### Функция обновления отзывов
//
function updateTestimonials() {
    const {name, photoUrl, text} = testimonials[idx]

    imgEl.src = photoUrl;
    textEl.innerText = text;
    userNameEl.innerText = name;

    idx++;
    if (idx === testimonials.length){
        idx = 0;
    }

    setTimeout(() => {
        updateTestimonials()
    }, 2000)
}
//
// - **`const {name, photoUrl, text} = testimonials[idx]`**: Деструктуризация объекта из массива `testimonials` на основе текущего индекса `idx`. Это позволяет извлечь `name`, `photoUrl` и `text` текущего отзыва.
//
// - **`imgEl.src = photoUrl`**: Обновляет `src` атрибут элемента изображения, устанавливая его на `photoUrl`, чтобы отобразить фото пользователя.
//
// - **`textEl.innerText = text`**: Обновляет текстовый контент элемента с отзывом, устанавливая его на `text`.
//
// - **`userNameEl.innerText = name`**: Обновляет текстовый контент элемента с именем пользователя, устанавливая его на `name`.
//
// - **`idx++`**: Увеличивает индекс на 1 для перехода к следующему отзыву.
//
// - **`if (idx === testimonials.length){ idx = 0; }`**: Если индекс достигает длины массива `testimonials`, он сбрасывается на `0`, что позволяет начинать цикл с начала.
//
// - **`setTimeout(() => { updateTestimonials() }, 2000)`**: Устанавливает таймер на 2 секунды, после которого функция `updateTestimonials` будет вызвана снова, что обеспечивает непрерывное циклическое обновление отзывов.
//
// ### Обобщение
//
// Этот JavaScript код создает простой слайдер для отзывов. Основные этапы его работы:
//
//     1. Получает массив отзывов и элементы DOM, которые нужно обновлять.
// 2. Инициализирует индекс текущего отзыва.
// 3. Функция `updateTestimonials` обновляет содержимое страницы на основе текущего отзыва и вызывает саму себя через 2 секунды для показа следующего отзыва.
//
//     Таким образом, каждый отзыв отображается на экране на 2 секунды, после чего показывается следующий. Этот процесс продолжается бесконечно.